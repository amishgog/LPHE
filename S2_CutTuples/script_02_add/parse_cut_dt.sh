#========================================================================
#========================================================================
Input=../root/data_${1}_${2}_precut.root
Ouput=../root/data_${1}_${2}_precut_add.root
echo "------------------------"
echo "Add variables to ${Input}"

lb-conda default root -l --web=off -b  << EOF
.L make_add_dt_C.so
make("${Input}", "${Ouput}")
EOF

echo "Done ${Input}"
#========================================================================
#========================================================================
