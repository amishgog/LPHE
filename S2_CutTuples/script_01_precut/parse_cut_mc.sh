#========================================================================
#========================================================================
Input=../../S1.1_Truth_PIDCr/root/mc_${1}_${2}_PIDCorr.root
Ouput=../root/mc_${1}_${2}_precut.root
KS=${1}
echo "------------------------"
echo "Cut ${Input}"

lb-conda default root -l --web=off -b  << EOF
.L copytree_presel_C.so
copytree_presel("${Input}", "${Ouput}", "${KS}", 1)
EOF

echo "Done ${Input}"
#========================================================================
#========================================================================
