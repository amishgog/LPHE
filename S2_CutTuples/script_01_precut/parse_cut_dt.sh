#========================================================================
#========================================================================
Input=../../S1_CleanTuples/root/data_${1}_${2}_Clean.root
Ouput=../root/data_${1}_${2}_precut.root
KS=${1}
echo "------------------------"
echo "Cut ${Input}"

lb-conda default root -l --web=off -b  << EOF
.L copytree_presel_C.so
copytree_presel("${Input}", "${Ouput}", "${KS}", 0)
EOF

echo "Done ${Input}"
#========================================================================
#========================================================================
